syntax = "proto3";

service Chat{
    rpc ConversaChat (Retorno) returns (stream Mensagem);
    rpc EnviaMensagem (Mensagem) returns (Retorno);
}

message Mensagem{
    string dono = 1;
    string conteudo = 2;
}

message Retorno{}